

/** 初始化课次时间选择器
 * @param refServerTime 服务器端的时间
 * @param refContainerId 容器编号
 */
function initLessonTimePicker(refServerTime, refContainerId) {
	var currentMonth = refTime.getMonth();
	var monthArray = new Array();
	for (var n=0; n<3; n++) {
		var curMonthData = new Object();
		curMonthData.value = currentMonth;
		
	}

}